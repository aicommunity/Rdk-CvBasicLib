# [ComponentName]

## RU

**Класс**: `[ComponentName]` — [краткое описание].
**Регистрация**: `Core/Basic.cpp` → `UploadClass("[ComponentName]", ...)`.
**Базовый класс**: (см. заголовок в `Core/`).

### Свойства / методы
Сверяйте таблицы с заголовками `Core/*.h` при доработке.

### Ключевые свойства / Favorites
Таблица primary-свойств для `Bin/ClDesc/.../Favorites` (`{CompName}:Prop`). Secondary (`Activity`, `Coord`, `Name`, `TimeStep`, debug…) не включать. См. `Docs/ClDesc-Detailed-Methodology.md`.

### Использование
См. [Usage-Examples.md](Usage-Examples.md) и `Bin/Configs/` при наличии.

## Sources
См. каталог библиотеки.

---

## EN

**Class**: `[ComponentName]` — [short description].
**Registration**: `Core/Basic.cpp` → `UploadClass("[ComponentName]", ...)`.
**Base class**: see header under `Core/`.

### Properties / methods
Keep tables synced with `Core/*.h` headers when enriching.

### Usage
See [Usage-Examples.md](Usage-Examples.md) and `Bin/Configs/` when available.
